---
import Icon from "../components/Icon.astro";

const currentYear = new Date().getFullYear();
---

<footer>
    <div>
        <div>
            <h1>Contact</h1>
            <p>Baron de Vironlaan 117,<br />1700 Dilbeek</p>
            <a class="anchor-light" href="contact">Vragen? Contacteer ons</a>
        </div>
        <div>
            <h1>Sitemap</h1>
            <ul>
                <li><a class="anchor-light" href="/">Home</a></li>
                <li><a class="anchor-light" href="/artist">Kunstenaars</a></li>
                <li><a class="anchor-light" href="/art">Kunstwerken</a></li>
                <li><a class="anchor-light" href="/expo">Exposities</a></li>
                <li><a class="anchor-light" href="/over-ons">Over ons</a></li>
                <li><a class="anchor-light" href="/contact">Contact</a></li>
            </ul>
        </div>
        <div>
            <h1>Blijf op de hoogte</h1>
            <p>
                Geef hieronder uw e-mailadres op om u in te schrijven in onze
                nieuwsbrief, en blijft op de hoogte van onze activiteiten.
            </p>
            <form class="form-dark">
                <input type="text" placeholder="E-mailadres*" />
                <button type="submit">Registreer</button>
            </form>
        </div>
    </div>
    <div>
        <p>
            Copyright {currentYear} &copy; Art-line
        </p>
        <p>
            <Icon icon="facebook-logo" size="1.25em" />
            <Icon icon="instagram-logo" size="1.25em" />
        </p>
    </div>
</footer>

<style lang="scss">
    footer {
        padding: 2.5em 5em;
        background-color: var(--accent-3);
        color: var(--white);

        > :nth-child(1) {
            display: flex;
            flex-wrap: wrap;

            > :nth-child(1) {
                width: calc(3 / 12 * 100%);

                @media (max-width: 768px) {
                    width: 100%;
                }
            }

            > :nth-child(2) {
                width: calc(3 / 12 * 100%);

                @media (max-width: 768px) {
                    width: 100%;
                }
            }

            > :nth-child(3) {
                width: calc(6 / 12 * 100%);

                @media (max-width: 768px) {
                    width: 100%;
                }

                > form {
                    display: flex;
                    flex-wrap: wrap;
                    gap: 1.25em;

                    > :nth-child(1) {
                        width: calc(60% - 1.25em * 1 / 2);
                    }

                    > :nth-child(2) {
                        width: calc(40% - 1.25em * 1 / 2);
                    }
                }
            }
        }

        > :nth-child(2) {
            margin-top: 5em;
            display: flex;
            align-items: center;
            border-top: 2px solid var(--white);

            > :nth-child(2) {
                margin-left: auto;
            }
        }
    }

    ul {
        list-style-type: none;
        padding: 0;
        display: flex;
        flex-direction: row;
        flex-wrap: wrap;
        row-gap: 1.25em;
    }

    li {
        width: 50%;
    }
</style>
